//SALVA STR1
//SALVA STR2

//STAMPA OUTPUT strcmp(str1, str2);


//strcmp(str1, str2);
    //se str1 <= str2
        a = str1
        b = str2
    //altrimenti
        a = str2
        b = str1
    
    //FINCHE' NON SIAMO ALLA FINE DI STR1
        if a[i] > b[i]
            return 1;
        else if a[i] < b[i]
            return -1;
        else
            i++;     
    
    if a < b 
        return -1;
    else if a > b
        return 1;
        
