//ALGORITHM FOR INTERSECTION ON ORDERED SETS

A1[L1] = {...};
A2[L2] = {...};

ptrA = min(A1, A2);
ptrB = max(A1, A2);

minEl = ptrA[0];
maxEl = ptrA[L];


i=0
while( ptrB[i] >= minEl && ptrB[i] <= maxEl){
	
	
}