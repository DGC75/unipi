MERGE(A, p, q, r){
    
    n1 = q - p + 1;
    n2 = r - q;

    L[n1 +1];
    R[n2 +1];

    for(i = 1 to n1 )
        L[i] = A[p + i - 1];

    for(i = 1 to n2)
        R[i] = [q + i];

    L[n1 +1] = +∞;    
    R[n2 +1] = +∞; 

    i = 1
    j = 1
    while(L[i] < )
}

